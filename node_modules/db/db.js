const
    pgp = require('pg-promise')(),
    db = pgp(process.env.DATABASE_URL);
pgp.pg.defaults.poolSize = 10;

//converting strings to ints
pgp.pg.types.setTypeParser(20, value => + value);
module.exports = db;